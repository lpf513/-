# 《合成奇境》实现说明（第三轮补全）

## 本轮完成
- **离线求解器（BFS）**：新增 `BFSSolver`，用于“可解性 + 唯一解”校验。
- **DSL强校验升级**：`LevelDSLValidator` 已接入求解器，支持不可解/多解检查。
- **监控落库能力**：新增 `MetricsStore`（SQLite），可把本地事件持久化。
- **监控与告警函数**：新增基础告警评估（广告转化率、卡点触发率）。
- **广告SDK抽象层**：新增 `RewardedAdAdapter`，供后续微信/平台SDK对接。
- **引擎接入SDK抽象**：看广告决策已走 `ad_adapter.show()` 流程。

## 当前覆盖状态
- 已提供离线批处理扫描脚本：`scripts/scan_levels.py`。
1. 状态机 + 决策分支：已完成。
2. 广告策略 + 合规约束：已完成。
3. 远程配置 + AB分桶：已完成。
4. DSL校验 + 可解性基础验证：已完成（BFS简化版）。
5. 事件埋点 + SQLite落库：已完成。
6. 实时大盘与生产数据链路：待接入线上数据源。

## 下一阶段
- 把 BFS 升级为“完整规则求解器”（含障碍格/锁定格/特殊格/生成规则）。
- 接入真实广告SDK回调（开始/完成/中断/奖励校验）。
- 增加关卡离线批处理扫描脚本（批量输出可解率、唯一解率、目标步数分布）。
